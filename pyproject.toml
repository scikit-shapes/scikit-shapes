[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "skshapes"
version = "0.3.1"
description = "Shape analysis in python"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">= 3.10"
dependencies = [
    "torch",
    "torchdiffeq",
    "numpy",
    "pykeops",
    "geomloss",
    "jaxtyping",
    "beartype",
    "pyvista",
    "vedo",
    "fast-simplification",
    "fast-edges-extraction",
    "scikit-learn",
    "pyamg",
]

# Optional dependencies: see https://github.com/scikit-shapes/scikit-shapes/pull/55
[project.optional-dependencies]
taichi = ["taichi"]
all = ["skshapes[taichi]"] # https://stackoverflow.com/questions/75490231/pip-pyproject-toml-can-optional-dependency-groups-require-other-optional-depend

[dependency-groups]
test = [
  "black",
  "flake8",
  "hypothesis",
  "pre-commit",
  "pytest",
  "pytest-cov",
  "pytest-sphinx @ git+https://github.com/jeanfeydy/pytest-sphinx.git",
]
dev = [
  { include-group = "test" },
]
docs = [
  "furo",
  "myst_parser>=0.13",
  # To generate GIFs
  "imageio",
  # Sphinx + theme/plugins
  "sphinx>=4.0",
  "sphinx-autodoc-typehints",
  "sphinx-copybutton",
  "sphinx-design",
  "sphinx-gallery<= 0.15.0",
  "sphinx-math-dollar",
  "sphinx-rtd-theme",
  # To generate interactive scenes
  "ipython",
  "jupyterlab",
  "trame",
  "trame_vtk",
  "trame_vuetify",
  # For multiscale registration example, could be nice to have another implementation of
  # projection on surface mesh in the future
  "trimesh",
  "rtree",
]

[project.urls]
Documentation = "https://scikit-shapes.github.io/scikit-shapes/"
Repository = "https://github.com/scikit-shapes/scikit-shapes"

[tool.black]
line-length = 79
target-version = ["py311", "py312", "py313"]

[tool.pytest.ini_options]
log_level = "INFO"
minversion = "6.0"
addopts = [
    "-ra",
    "--showlocals",
    "--strict-markers",
    "--strict-config",
    "--cov=skshapes",
    "--doctest-modules",
    "--cov-report=term-missing",
    "--cov-report=html",
    "./tests",
    "./src",
    ]
xfail_strict = true

# Importing taichi leads to a locale.getdefaultlocale deprecation warning, we ignore it
# See https://stackoverflow.com/questions/58399870/pytest-suppress-deprecationwarning-from-specific-3rd-party-modules/69622325#69622325
# for dtails about filterwarnings configuration
filterwarnings = [
  "error",
  "ignore:'locale.getdefaultlocale' is deprecated*:DeprecationWarning",
  "ignore:support for supplying keyword arguments to pathlib.PurePath is deprecated*:DeprecationWarning"
]


log_cli_level = "info"
testpaths = [
  "tests",
  "optional_tests"
  ]

[tool.repo-review]
ignore = [
    "PY004", # doc/ instead of docs/
    "PC140", # MyPy
    "PC170", # PyGrep (because no rst files)
    "PC180", # Prettier
    "MY", # MyPy
    "RTD", # ReadTheDocs
    "RF003", # Let me be explicit about the "src" directory in the config below
    ]


# See: https://learn.scientific-python.org/development/guides/style/#PC190
[tool.ruff]
src = ["src"]

[tool.ruff.lint]
extend-select = [
  "B",        # flake8-bugbear
  "I",        # isort
  "ARG",      # flake8-unused-arguments
  "C4",       # flake8-comprehensions
  "EM",       # flake8-errmsg
  "ICN",      # flake8-import-conventions
  "G",        # flake8-logging-format
  "PGH",      # pygrep-hooks
  "PIE",      # flake8-pie
  "PL",       # pylint
  "PT",       # flake8-pytest-style
  "PTH",      # flake8-use-pathlib
  "RET",      # flake8-return
  "RUF",      # Ruff-specific
  "SIM",      # flake8-simplify
  "T20",      # flake8-print
  "UP",       # pyupgrade
  "YTT",      # flake8-2020
  "EXE",      # flake8-executable
  "NPY",      # NumPy specific rules
  "FURB",     # refurb
  "PYI",      # flake8-pyi
]
ignore = [
  "PLR09",    # Too many <...>
  "PLR2004",  # Magic value used in comparison
  "ISC001",   # Conflicts with formatter
  "PYI041",   # Use `float` instead of `int | float
  "RET505",   # Unnecessary `elif`/`else` after `return` statement
  "C408",     # Allow for dict(a=1) instead of {"a": 1}
  "PLC0415",  # Allow for local imports to avoid circular imports
  "PGH004",   # Allow ruff: noqa
]
typing-modules = ["skshapes.types"]

[tool.ruff.lint.per-file-ignores]
# For _init__.py files:
"**_init__.py**" = ["F401", "F403", "F405"]
"examples/**" = ["T201"]
# F401: imported but unused
# F403: unable to detect undefined names (import *)
# F405: may be undefined, or defined from star imports

# For examples gallery files:
"**examples/**" = ["RUF003", "E402"]
# RUF003: Ambiguous no-break spaces
# E402: module level import not at top of file

[tool.cibuildwheel]
build = ["cp310-manylinux*", "cp311-manylinux*"]

[tool.cibuildwheel.linux]
archs = ["x86_64"]
